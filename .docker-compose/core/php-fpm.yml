services:
  php:
    build:
      context: ../../php-fpm
      args:
        PHP_VERSION: 8.4
    container_name: symfony_php
    ports:
      - "9000:9000"
    volumes:
      - ../../app/:/app/
      - ../../php-fpm/xdebug.ini:/usr/local/etc/php/conf.d/docker-php-ext-xdebug.ini
    networks:
      app_net:
