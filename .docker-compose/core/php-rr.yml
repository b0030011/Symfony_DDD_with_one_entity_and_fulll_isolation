services:
  php:
    build:
      context: ../../roadrunner
      args:
        PHP_VERSION: 8.4
        ANTISLEEP_ENV: local
        PHP_INI_ENV: development
    container_name: symfony_php
    volumes:
      - ../../app/:/app/
    ports:
      - "8080:80"
    networks:
      app_net: