# http://pgconfig.org

# Generated by PGConfig 3.1.2 (5c1bbed5df8b2e1a63ef02bd759ef59016a923cc)
# https://api.pgconfig.org/v1/tuning/get-config?format=conf&&log_format=csvlog&max_connections=100&pg_version=12&environment_name=WEB&total_ram=64GB&cpus=4&drive_type=SSD&arch=x86-64&os_type=unix

# Memory Configuration
shared_buffers = 16GB
effective_cache_size = 48GB
work_mem = 492MB
maintenance_work_mem = 3GB

temp_buffers = 256MB


# Checkpoint Related Configuration
min_wal_size = 2GB
max_wal_size = 3GB
checkpoint_completion_target = 0.9
wal_buffers = -1

# Network Related Configuration
listen_addresses = '*'
max_connections = 50

# Storage Configuration
seq_page_cost = 1.1
random_page_cost = 1.1
effective_io_concurrency = 200

# Worker Processes
max_worker_processes = 8
max_parallel_workers_per_gather = 2
max_parallel_workers = 2


# Для повышения скорости работы с базой отключаем синхронные комиты, нельзя ставить это на боевом сервере
synchronous_commit = off
fsync = off
full_page_writes = off
row_security = off
